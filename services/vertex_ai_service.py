import os
import json
from config import Config

class VertexAIService:
    def __init__(self):
        # We don't need to load anything here anymore
        pass

    def build_system_prompt(self):
        # Return an empty string or just a tiny reminder
        return "You are Aura. Listen first."
